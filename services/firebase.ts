// Firebase service has been removed as requested.
// This file is kept as a placeholder to prevent import errors in older cached versions,
// but the functionality is now fully local.
export const firebaseConfig = {};
export const DEFAULT_CONFIG_JSON = "{}";
